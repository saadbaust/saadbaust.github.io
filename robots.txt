# Allow everything.
#
# SEO note: data.json, content.json and publications.bib are NOT blocked here,
# and that's intentional, not an oversight. Google's crawler renders the page's
# JavaScript before indexing content, and the page fetches all three files at
# runtime to show the live publication list (from publications.bib) and every
# structured section (from data.json + content.json). If you ever add a Disallow
# line for any of them, Googlebot's renderer will fail to fetch it and those
# parts of the page will effectively go un-indexed.
User-agent: *
Allow: /

Sitemap: https://saadbaust.github.io/sitemap.xml
